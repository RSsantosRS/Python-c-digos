while True:
    nome= input("nome: ")
    if len(nome) == 0:
        continue
    if nome == '-1':
        break
    n1= float(input("Nota da primeira prova: "))
    n2= float(input("nota da primeira prova: "))
    med = (n1+n2)/2
    if med >=7:
        print(f"O aluno {nome} foi aprovado com media {med}")
    else:
        print(f"O aluno {nome} foi reprovado com media {med}")
